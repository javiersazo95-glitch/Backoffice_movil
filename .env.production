# URL de producción del backend. Placeholder: confirmar con el equipo backend
# la URL real desplegada (probablemente Railway) antes de generar el build
# de producción.
EXPO_PUBLIC_API_URL=https://api-dev.repuestop.cl/api/v1

